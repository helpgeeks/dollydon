document.addEventListener('DOMContentLoaded',()=>{const reveal=[...document.querySelectorAll('[data-reveal]')];reveal.forEach(el=>el.classList.add('reveal'));const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.16});reveal.forEach(el=>io.observe(el));const a=document.querySelector('.ambient-a'),b=document.querySelector('.ambient-b');window.addEventListener('pointermove',e=>{const x=e.clientX/innerWidth-.5,y=e.clientY/innerHeight-.5;if(a)a.style.transform=`translate(${x*22}px,${y*16}px)`;if(b)b.style.transform=`translate(${x*-28}px,${y*-20}px)`},{passive:true});const form=document.querySelector('.challenge-form');if(form){const status=form.querySelector('.form-status'),button=form.querySelector('button');form.addEventListener('submit',async e=>{e.preventDefault();button.disabled=true;button.firstChild.textContent='Sending ';try{const body=new URLSearchParams(new FormData(form));const res=await fetch('/',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body});if(!res.ok)throw new Error();form.reset();button.firstChild.textContent='You’re In ';status.textContent='Thank you. Your challenge entry is confirmed.'}catch(err){button.disabled=false;button.firstChild.textContent='Try Again ';status.textContent='Something went wrong. Please try again.'}})}});
